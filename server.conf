server {
	listen 127.0.0.1:8888;
	client_max_body_size 1M;
	error_page 404 405 412 /4xx.html;
	location / {
		root /var/www/html;
		index index.html;
		autoindex on;
	}
	location .php {
		root /cgi-bin;
		cgi_param SCRIPT_FILENAME
		cgi_param QUERY_STRING
		cgi_param REQUEST_METHOD
		cgi_param CONTENT_TYPE
		cgi_param CONTENT_LENGTH
		cgi_param PATH_INFO
	}
}